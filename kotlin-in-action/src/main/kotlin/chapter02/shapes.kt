package chapter02

class Circle()

class Triangle()

// 여러개의 클래스를 한 파일에 위치시킬 수 있다.
// 따라서 추가적인 폴더가 필요 없게 된다.

// 자바라면, shapes 디렉토리를 하나 생성한 뒤 클래스들을 해당 디렉토리에 위치시켰을 것.
// 적절히 작은 구조라면, 한 파일에 클래스를 위치시켜도 좋다. 그러나 너무 많이 위치시킨다면 자바와의 호환이 좋지 않을 수 있음.
